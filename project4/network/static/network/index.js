document.addEventListener('DOMContentLoaded', function () {
    // like button
    likeButton = document.getElementById('likeIcon');
    likeButton.addEventListener('click', function () {
        // Change svg to filled heart
        // Increment like count

    });
});